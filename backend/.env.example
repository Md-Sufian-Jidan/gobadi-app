# Database connection configurations
DB_HOST=    
DB_PORT=    
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=

# Redis cache connection configurations
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
REDIS_TLS=

# JWT authentication configuration
JWT_SECRET=
JWT_EXPIRES_IN=

# OAuth configurations (required for Google/Facebook sign-in; create these in
# Google Cloud Console / Meta for Developers - not committed here)
GOOGLE_CLIENT_ID_ANDROID=
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=

# Cloudinary configurations for bulk image storage (optional, uses fallback if blank)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Nodemailer SMTP configurations (Optional, falls back to mock console logs if empty)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
OPENWEATHER_API_KEY=
MEILI_MASTER_KEY=
MEILISEARCH_API_KEY=